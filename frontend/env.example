# Frontend environment variables
# Copy to .env.local for local development. For Vercel, set in Project Settings → Environment Variables.

# Backend API base URL (include /api/v1). Required in production.
# Leave unset for local dev (defaults to http://localhost:3001/api/v1).
# NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app/api/v1
